# ASSUMPTIONS

> 目标：集中管理当前版本所有关键假设，支持交接与后续迭代追踪。

## 1. 使用说明
- 每条假设必须唯一 `ID`，并标注状态：`Active` / `Deprecated`。
- 每条假设必须可追溯到至少一个模块与参数名。
- 任何默认策略变更都要在“变更日志”登记。

## 2. 假设记录模板
| ID | 类型(论文/工程) | 描述 | 默认值/规则 | 影响模块 | 风险 | 验证方式 | 状态 |
|---|---|---|---|---|---|---|---|
| A-001 | 工程 | TODO | TODO | TODO | TODO | TODO | Active |

## 3. 假设分组（待填）

### 3.1 数据与输入假设
- TODO: 输入图像灰度语义、尺寸范围、目录结构约定。

### 3.2 光照校正假设
- TODO: `N` 奇数约束、`c` 量纲、边界模式默认。

### 3.3 多方向 top-hat 假设
- TODO: 改进公式、`M=9` 方向集合、`L` 离散化。

### 3.4 单尺度/双尺度流程假设
- TODO: 下采样与上采样插值、再二值化策略、OR 融合编码。

### 3.5 无监督统计与异常规则假设
- TODO: IQR/分位数阈值、score 判定、review 清单规则。

### 3.6 CLI 与输出产物假设
- TODO: 文件命名、异常跳过策略、快照与 CSV 落盘顺序。

## 4. 变更日志（待填）
| 日期 | 变更类型(新增/修改/废弃) | ID | 说明 | 负责人 |
|---|---|---|---|---|
| TODO | TODO | TODO | TODO | TODO |

## 5. 交接检查清单
- [ ] 所有 Active 假设均已映射到具体模块
- [ ] 所有默认参数均在 `final.yaml` 或 `default.yaml` 可追溯
- [ ] 已标记高风险假设并给出验证计划
- [ ] Deprecated 假设已注明替代策略
